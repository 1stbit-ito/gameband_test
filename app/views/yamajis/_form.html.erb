<%= form_for(@yamaji) do |f| %>
  <% if @yamaji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yamaji.errors.count, "error") %> prohibited this yamaji from being saved:</h2>

      <ul>
      <% @yamaji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mail %><br>
    <%= f.text_field :mail %>
  </div>
  <div class="field">
    <%= f.label :updated_at %><br>
    <%= f.datetime_select :updated_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
